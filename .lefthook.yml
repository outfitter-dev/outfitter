# Lefthook configuration for Outfitter Kit
# https://github.com/evilmartians/lefthook

pre-commit:
  parallel: true
  commands:
    format-check:
      glob: "*.{js,ts,tsx,json,md}"
      run: bunx biome format --no-errors-on-unmatched {staged_files}
      stage_fixed: true

    lint:
      glob: "*.{js,ts,tsx}"
      run: bunx biome lint --no-errors-on-unmatched {staged_files}

    typecheck:
      glob: "*.{ts,tsx}"
      run: bun run typecheck

pre-push:
  parallel: false
  commands:
    test:
      run: bun run test

    # ast-grep patterns (to be configured when patterns are defined)
    # ast-grep:
    #   run: bunx @ast-grep/cli scan --rule .ast-grep/rules/
