{
  "changesets": [
    {
      "releases": [
        {
          "name": "outfitter",
          "type": "minor"
        }
      ],
      "summary": "Add `outfitter check action-registry` command that cross-references command files in `apps/outfitter/src/commands/` against action definition imports in `apps/outfitter/src/actions/`. Reports unregistered command files with file paths. Supports `--output json|jsonl` for structured output. Exits with code 0 when all command files are referenced, code 1 when gaps are found.",
      "id": "add-action-registry-scanner"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "minor"
        }
      ],
      "summary": "Add `.context(factory)` method to `CommandBuilder` for async context construction. The factory receives typed `TInput` (post-schema validation from `.input()`) or raw parsed flags (when `.input()` is not used) and returns a typed context object. The context is passed to the `.action()` handler alongside input via `ctx`. Context factory errors are caught and produce proper exit codes using the error taxonomy. Generic `TContext` type flows through `CommandBuilder` to the action handler for full type safety.",
      "id": "add-context-factory"
    },
    {
      "releases": [
        {
          "name": "@outfitter/mcp",
          "type": "minor"
        }
      ],
      "summary": "Enhance `defineResourceTemplate()` with Zod schema validation for URI template parameters. When `paramSchema` is provided, URI template variables are validated and coerced before handler invocation — parallel to how `defineTool()` validates input. Add `TypedResourceTemplateDefinition<TParams>` type with typed handler. Backward compatible — existing usage without `paramSchema` works unchanged.",
      "id": "add-define-resource"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "minor"
        }
      ],
      "summary": "Add `.destructive(isDest)` to `CommandBuilder` for marking commands as destructive. When `destructive(true)` is set, a `--dry-run` boolean flag is auto-added to the command (deduplicated if already present from `.option()` or `.preset()`). Add `dryRun` option to `runHandler()` — when `true`, the success envelope includes a CLIHint with the command to execute without `--dry-run` (preview-then-commit pattern). The handler is responsible for checking the dry-run flag and performing preview-only logic.",
      "id": "add-destructive-dry-run"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "minor"
        }
      ],
      "summary": "Add `output.envelope()` and `runHandler()` lifecycle bridge. `createSuccessEnvelope()` and `createErrorEnvelope()` wrap command results in a structured `{ ok, command, result/error, hints? }` envelope. The `hints` field is absent (not empty array) when there are no hints. `runHandler()` bridges the full CommandBuilder lifecycle: context factory → handler invocation → Result unwrap → envelope construction → output formatting → exit code mapping. Success produces `ok: true` with result; failure produces `ok: false` with error category and message. Exit codes come from the error taxonomy `exitCodeMap`. Exported from `@outfitter/cli/envelope` subpath.",
      "id": "add-envelope-run-handler"
    },
    {
      "releases": [
        {
          "name": "outfitter",
          "type": "minor"
        },
        {
          "name": "@outfitter/presets",
          "type": "minor"
        }
      ],
      "summary": "Add `--example <name>` flag to `outfitter init cli` and `outfitter init mcp` commands. `outfitter init cli my-tool --example todo` scaffolds a pattern-rich todo-list CLI demonstrating builder patterns (`.input()`, `.context()`, `.hints()`, `runHandler()`). `outfitter init mcp my-server --example files` scaffolds an MCP server with `defineResource()`, `defineResourceTemplate()`, and `defineTool()` patterns. Examples use real v0.5 patterns — not hello-world stubs.",
      "id": "add-example-flag-init"
    },
    {
      "releases": [
        {
          "name": "@outfitter/contracts",
          "type": "minor"
        }
      ],
      "summary": "Add `expectOk(result)` and `expectErr(result)` test assertion helpers. `expectOk` asserts a Result is Ok and returns the narrowed `T` value; throws a descriptive error if Err. `expectErr` does the inverse — asserts Err and returns narrowed `E`. Both accept an optional message parameter and work with Bun's test runner.",
      "id": "add-expect-ok-err"
    },
    {
      "releases": [
        {
          "name": "@outfitter/contracts",
          "type": "minor"
        }
      ],
      "summary": "Add `fromFetch(response)` helper that converts an HTTP Response into a `Result<Response, OutfitterError>`. Maps HTTP status codes to error categories per the taxonomy: 2xx → Ok, 404 → not_found, 401 → auth, 429 → rate_limit, 403 → permission, 408/504 → timeout, 502/503 → network, 409 → conflict, other 5xx → internal.",
      "id": "add-from-fetch"
    },
    {
      "releases": [
        {
          "name": "@outfitter/contracts",
          "type": "minor"
        }
      ],
      "summary": "Add `ActionHint`, `CLIHint`, and `MCPHint` types for agent-navigable responses. Exported from `@outfitter/contracts` root and `@outfitter/contracts/hints` subpath. Types only — no runtime code.",
      "id": "add-hint-types"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "minor"
        }
      ],
      "summary": "Add `.hints(fn)` and `.onError(fn)` to CommandBuilder for transport-local hint declarations. Success hint function receives `(result, input)` and error hint function receives `(error, input)`, both returning `CLIHint[]`. Handlers remain transport-agnostic — hint functions are stored on the builder and invoked at output time by `runHandler()`.",
      "id": "add-hints-on-error"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "minor"
        }
      ],
      "summary": "Add `.input(schema)` method to `CommandBuilder` for Zod-to-Commander auto-derive. Accepts a Zod object schema and auto-derives Commander flags: `z.string()` → string option, `z.number()` → number option with coercion, `z.boolean()` → boolean flag, `z.enum()` → choices option. `.describe()` text becomes option descriptions, `.default()` values become option defaults. Explicit `.option()`/`.requiredOption()` calls compose alongside `.input()` — they override or supplement auto-derived flags. The `.action()` handler receives a validated, typed `input` object when `.input()` is used.",
      "id": "add-input-schema"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "minor"
        },
        {
          "name": "@outfitter/mcp",
          "type": "minor"
        },
        {
          "name": "@outfitter/contracts",
          "type": "minor"
        }
      ],
      "summary": "Add `.readOnly(bool)` and `.idempotent(bool)` methods to `CommandBuilder` for declaring safety metadata on commands. When set to `true`, these signals are included in the self-documenting root command tree (JSON mode) under a `metadata` field. In the MCP package, `buildMcpTools()` maps `readOnly` to `readOnlyHint` and `idempotent` to `idempotentHint` in MCP tool annotations via the `ActionMcpSpec` interface. Default is non-read-only, non-idempotent (metadata omitted when not set).",
      "id": "add-metadata-annotations"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "minor"
        }
      ],
      "summary": "Add output truncation with pagination hints and file pointers\n\n- New `truncation.ts` module: `truncateOutput()` function for truncating array output with configurable `limit` and `offset`\n- Without `limit`, output passes through untouched (off by default)\n- When data exceeds limit: truncates to limit items with `{ showing, total, truncated: true }` metadata\n- Generates pagination `CLIHint(s)` for continuation (`--offset`, `--limit`)\n- For very large output (>1000 items by default), writes full result to a temp file and includes `{ full_output: path }` file pointer\n- File write failures degrade gracefully (returns truncated output with warning hint, no crash)\n- Structured output (JSON/JSONL) remains parseable after truncation\n- File paths constrained to safe OS temp directories",
      "id": "add-output-truncation"
    },
    {
      "releases": [
        {
          "name": "@outfitter/contracts",
          "type": "minor"
        }
      ],
      "summary": "Add `parseInput(schema, data)` Zod-to-Result helper. Wraps `safeParse` into `Result<T, ValidationError>` where `T` is automatically inferred from the schema — no manual type argument needed.",
      "id": "add-parse-input"
    },
    {
      "releases": [
        {
          "name": "@outfitter/contracts",
          "type": "minor"
        }
      ],
      "summary": "Add transport-agnostic streaming types: `StreamEvent` discriminated union (`StreamStartEvent`, `StreamStepEvent`, `StreamProgressEvent`), `ProgressCallback` type, and optional `progress` field on `HandlerContext`. Handlers can call `ctx.progress?.()` to emit progress events without knowing which transport consumes them.",
      "id": "add-progress-types"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "patch"
        },
        {
          "name": "@outfitter/contracts",
          "type": "patch"
        },
        {
          "name": "@outfitter/mcp",
          "type": "patch"
        }
      ],
      "summary": "Add standalone reference project in examples/ demonstrating v0.4-v0.6 integration",
      "id": "add-reference-project"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "minor"
        }
      ],
      "summary": "Add `.relatedTo(target, options?)` to `CommandBuilder` for declaring relationships between commands. Declarations build a navigable action graph (tier-4 hints). `buildActionGraph()` constructs graph nodes (commands) and edges (relationships) from a Commander program. `graphSuccessHints()` generates next-action hints from graph neighbors for success envelopes. `graphErrorHints()` generates remediation-path hints for error envelopes. Unknown targets produce warnings (not crashes), self-links and cycles are handled safely.",
      "id": "add-related-to-action-graph"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "minor"
        }
      ],
      "summary": "Surface `retryable` and `retry_after` in error envelopes. Error envelopes now include `retryable: boolean` derived from `errorCategoryMeta()`, indicating whether the error is transient and safe to retry. For `rate_limit` errors with `retryAfterSeconds`, the envelope also includes `retry_after` in seconds. Non-delay errors omit `retry_after`.",
      "id": "add-retry-envelope"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "minor"
        }
      ],
      "summary": "Add schema-driven presets via `createSchemaPreset()` and refactor `.preset()` to accept both `FlagPreset` and `SchemaPreset`. Schema presets use Zod schema fragments for auto-deriving Commander flags (same as `.input()`), eliminating the need for manual option declarations. Preset schemas compose with `.input()` — merged flags include both command input fields and preset fields. Existing `FlagPreset`-based presets continue to work unchanged (backward compatible). Also exports `isSchemaPreset()` type guard and `AnyPreset` union type.",
      "id": "add-schema-presets"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "minor"
        }
      ],
      "summary": "Add self-documenting root command and hint generation tiers. When no subcommand is given, `createCLI()` outputs the full command tree as JSON (piped/JSON mode) or help text (TTY mode). The command tree includes all registered commands with descriptions and available options. Implement three hint generation tiers: Tier 1 (`commandTreeHints()`) auto-generates CLIHint[] from the Commander command registry. Tier 2 (`errorRecoveryHints()`) produces standard recovery actions per error category using enriched ErrorCategory metadata (retryable flags). Tier 3 (`schemaHintParams()`) populates hint params from Zod input schemas. All functions exported from `@outfitter/cli/hints` subpath.",
      "id": "add-self-doc-hints"
    },
    {
      "releases": [
        {
          "name": "@outfitter/testing",
          "type": "minor"
        }
      ],
      "summary": "Add `testCommand()` and `testTool()` wiring test helpers. `testCommand(cli, args, options?)` wraps `captureCLI()` to execute CLI instances and return captured stdout, stderr, and exitCode without side effects. `testTool(tool, input, options?)` validates input against the tool's Zod schema and invokes the handler with a test context — invalid input returns a ValidationError without calling the handler.",
      "id": "add-test-command-tool"
    },
    {
      "releases": [
        {
          "name": "outfitter",
          "type": "minor"
        }
      ],
      "summary": "Add `outfitter upgrade codemod` command that transforms Commander `.command().action()` patterns to the builder pattern with `.input(schema).action()`. Generates Zod schema skeletons from existing `.option()` / `.argument()` declarations. Commands too complex for automatic transformation (nested subcommands, dynamic patterns) are left as-is and reported as skipped with `cli.register()` fallback. Supports `--dry-run` for previewing changes and `--output json|jsonl` for structured output.",
      "id": "add-upgrade-codemod-builder"
    },
    {
      "releases": [
        {
          "name": "@outfitter/contracts",
          "type": "minor"
        }
      ],
      "summary": "Add `wrapError(error, mapper?)` for normalizing unknown errors into typed OutfitterErrors. Typed errors pass through unchanged (same reference, mapper not called). Untyped errors go through optional mapper; unmatched errors wrap as InternalError. Also adds `composeMappers()` for composable mapper pipelines that short-circuit on first match, and `isOutfitterError()` type guard.",
      "id": "add-wrap-error"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "major"
        }
      ],
      "summary": "Move `commander` from `dependencies` to `peerDependencies` with `>=14.0.0`. Consumers must now provide their own `commander` installation.",
      "id": "commander-peer-dep"
    },
    {
      "releases": [
        {
          "name": "@outfitter/config",
          "type": "minor"
        }
      ],
      "summary": "Make `loadConfig()` schema parameter optional with TypeScript overloads. Without schema, returns raw parsed config as `Result<unknown, ...>` (unvalidated). With schema, returns validated typed config as `Result<T, ...>` (backward compatible). Overloads narrow the return type correctly: `loadConfig(appName)` → `Result<unknown>`, `loadConfig(appName, schema)` → `Result<T>`.",
      "id": "config-optional-schema"
    },
    {
      "releases": [
        {
          "name": "outfitter",
          "type": "patch"
        }
      ],
      "summary": "Fix `resolveDocsOutputMode` to use `hasExplicitOutputFlag` pattern so that `OUTFITTER_JSON=1` and `OUTFITTER_JSONL=1` env vars work for docs commands when no explicit `--output` flag is passed. Commander default values no longer suppress env var fallback.",
      "id": "docs-output-mode-fix"
    },
    {
      "releases": [
        {
          "name": "@outfitter/testing",
          "type": "minor"
        }
      ],
      "summary": "Enhanced testCommand() and testTool() with schema, context, and hints support for v0.5 builder pattern.\n\n**testCommand() enhancements:**\n\n- `input` option: Pre-parsed input object auto-converted to CLI args\n- `context` option: Mock context injectable via `getTestContext()` for context factories\n- `json` option: Force JSON output mode for envelope parsing\n- `envelope` field: Parsed `CommandEnvelope` from JSON output for structured assertion\n- Returns `TestCommandResult` (extends `CliTestResult` with `envelope`)\n\n**testTool() enhancements:**\n\n- `context` option: Full `HandlerContext` overrides (takes priority over individual cwd/env/requestId)\n- `hints` option: Hint generation function for asserting on MCP hints\n- Returns `TestToolResult` (extends `Result` with `hints`)\n\n**New exports:**\n\n- `getTestContext()`: Retrieve injected test context in context factories\n- `TestCommandResult` type\n- `TestToolResult` type\n\nAll changes are backward compatible — existing tests continue to work without modification.",
      "id": "enhance-test-helpers"
    },
    {
      "releases": [
        {
          "name": "@outfitter/contracts",
          "type": "minor"
        }
      ],
      "summary": "Add `jsonRpcCodeMap`, `retryableMap`, and `errorCategoryMeta()` to enrich ErrorCategory with JSON-RPC codes (for MCP protocol compliance) and retryable flags (for agent safety)",
      "id": "error-enrichment"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "patch"
        }
      ],
      "summary": "Fix `--json` global flag default from `false` to `undefined` so downstream code can distinguish \"not passed\" from \"explicitly disabled\". The preAction env bridge now only sets `OUTFITTER_JSON` when `--json` is explicitly passed.",
      "id": "fix-json-flag-default"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "patch"
        }
      ],
      "summary": "Fix 3 safety edge cases: (1) `.destructive()` is now order-agnostic with `.action()` — flag application deferred to `.build()` time. (2) `buildDryRunHint()` strips `--dry-run=true` and `--dry-run=<value>` variants. (3) `createErrorEnvelope()` only includes `retry_after` for `rate_limit` errors (defense-in-depth guard).",
      "id": "fix-safety-edge-cases"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "patch"
        }
      ],
      "summary": "Replace raw JSON.stringify with safeStringify in NDJSON streaming and error envelope serialization paths for consistent BigInt handling, circular reference detection, and sensitive data redaction",
      "id": "fix-streaming-safe-stringify"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "patch"
        }
      ],
      "summary": "Fix `validateInput()` to surface Zod validation errors via `exitWithError()` instead of silently falling through to raw input. When `.input(schema)` is used and schema validation fails, the command now exits with code 1 (validation category) and outputs a `ValidationError` with Zod issue details (field names, expected types, messages). Invalid data no longer reaches `.action()` handlers.",
      "id": "fix-validate-input-fallthrough"
    },
    {
      "releases": [
        {
          "name": "@outfitter/mcp",
          "type": "minor"
        }
      ],
      "summary": "Add MCP progress adapter translating ctx.progress StreamEvent to notifications/progress.\n\nWhen an MCP client provides a progressToken in tool call params, ctx.progress is now a\nProgressCallback (from @outfitter/contracts) that emits `notifications/progress` via the\nMCP SDK for each StreamEvent. Without a progressToken, ctx.progress remains undefined.\n\nThe adapter is a separate module (`progress.ts`) for modularity, parallel to the CLI\nNDJSON adapter in @outfitter/cli.",
      "id": "mcp-progress-adapter"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "patch"
        }
      ],
      "summary": "Fix 4 non-blocking edge cases: tempDir validation in truncateOutput() rejects unsafe paths (relative, traversal) with graceful fallback to OS tmpdir; buildActionGraph() now recursively traverses nested subcommands in group commands; cross-feature dry-run test reads actual flag from process.argv instead of hardcoded boolean; added optional truncation config on OutputOptions for future automatic truncation support.",
      "id": "misc-completeness-edge-cases"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "minor"
        }
      ],
      "summary": "Add NDJSON streaming adapter with `--stream` flag support\n\n- New `streaming.ts` module: `createNdjsonProgress()`, `writeNdjsonLine()`, `writeStreamEnvelope()` for writing progress events as NDJSON lines to stdout\n- New `streamPreset()` flag preset in `query.ts`: adds `--stream` boolean flag\n- `runHandler()` accepts `stream: true` option: emits start event, provides `ctx.progress` callback to handler, writes terminal envelope as last NDJSON line\n- `--stream` is orthogonal to output mode (`--output human|json|jsonl` and env vars)\n- Event ordering: start event first, progress/step events in middle, terminal envelope last",
      "id": "ndjson-streaming-adapter"
    },
    {
      "releases": [
        {
          "name": "@outfitter/cli",
          "type": "major"
        }
      ],
      "summary": "Add explicit `format` parameter to `output()` and `exitWithError()` for flag-driven format selection. The `mode` property has been removed from `OutputOptions`. Detection hierarchy (highest wins): (1) explicit `format` param, (2) env vars (`OUTFITTER_JSON`, `OUTFITTER_JSONL`), (3) default `\"human\"`. This is a breaking change to the function signatures.",
      "id": "output-format-param"
    },
    {
      "releases": [
        {
          "name": "@outfitter/tooling",
          "type": "patch"
        },
        {
          "name": "@outfitter/mcp",
          "type": "patch"
        },
        {
          "name": "@outfitter/contracts",
          "type": "patch"
        },
        {
          "name": "@outfitter/presets",
          "type": "patch"
        },
        {
          "name": "outfitter",
          "type": "patch"
        }
      ],
      "summary": "Land the stacked follow-up fixes across tooling, runtime, and scaffold packages after the repo-shape cleanup.",
      "id": "stack-pr-followups"
    }
  ],
  "releases": [
    {
      "name": "outfitter",
      "type": "minor",
      "oldVersion": "0.3.4",
      "changesets": [
        "add-action-registry-scanner",
        "add-example-flag-init",
        "add-upgrade-codemod-builder",
        "docs-output-mode-fix",
        "stack-pr-followups"
      ],
      "newVersion": "0.4.0"
    },
    {
      "name": "@outfitter/cli",
      "type": "major",
      "oldVersion": "0.5.3",
      "changesets": [
        "add-context-factory",
        "add-destructive-dry-run",
        "add-envelope-run-handler",
        "add-hints-on-error",
        "add-input-schema",
        "add-metadata-annotations",
        "add-output-truncation",
        "add-reference-project",
        "add-related-to-action-graph",
        "add-retry-envelope",
        "add-schema-presets",
        "add-self-doc-hints",
        "commander-peer-dep",
        "fix-json-flag-default",
        "fix-safety-edge-cases",
        "fix-streaming-safe-stringify",
        "fix-validate-input-fallthrough",
        "misc-completeness-edge-cases",
        "ndjson-streaming-adapter",
        "output-format-param"
      ],
      "newVersion": "1.0.0"
    },
    {
      "name": "@outfitter/mcp",
      "type": "minor",
      "oldVersion": "0.4.3",
      "changesets": [
        "add-define-resource",
        "add-metadata-annotations",
        "add-reference-project",
        "mcp-progress-adapter",
        "stack-pr-followups"
      ],
      "newVersion": "0.5.0"
    },
    {
      "name": "@outfitter/presets",
      "type": "minor",
      "oldVersion": "0.2.1",
      "changesets": [
        "add-example-flag-init",
        "stack-pr-followups"
      ],
      "newVersion": "0.3.0"
    },
    {
      "name": "@outfitter/contracts",
      "type": "minor",
      "oldVersion": "0.4.2",
      "changesets": [
        "add-expect-ok-err",
        "add-from-fetch",
        "add-hint-types",
        "add-metadata-annotations",
        "add-parse-input",
        "add-progress-types",
        "add-reference-project",
        "add-wrap-error",
        "error-enrichment",
        "stack-pr-followups"
      ],
      "newVersion": "0.5.0"
    },
    {
      "name": "@outfitter/testing",
      "type": "minor",
      "oldVersion": "0.2.5",
      "changesets": [
        "add-test-command-tool",
        "enhance-test-helpers"
      ],
      "newVersion": "0.3.0"
    },
    {
      "name": "@outfitter/config",
      "type": "minor",
      "oldVersion": "0.3.4",
      "changesets": [
        "config-optional-schema"
      ],
      "newVersion": "0.4.0"
    },
    {
      "name": "@outfitter/tooling",
      "type": "patch",
      "oldVersion": "0.3.4",
      "changesets": [
        "stack-pr-followups"
      ],
      "newVersion": "0.3.5"
    },
    {
      "name": "outfitter-cli-demo",
      "type": "patch",
      "oldVersion": "0.1.6",
      "changesets": [],
      "newVersion": "0.1.7"
    },
    {
      "name": "@outfitter/reference",
      "type": "patch",
      "oldVersion": "0.1.0",
      "changesets": [],
      "newVersion": "0.1.1"
    },
    {
      "name": "@outfitter/tui",
      "type": "none",
      "oldVersion": "0.2.2",
      "changesets": [],
      "newVersion": "0.2.2"
    },
    {
      "name": "@outfitter/daemon",
      "type": "patch",
      "oldVersion": "0.2.5",
      "changesets": [],
      "newVersion": "0.2.6"
    },
    {
      "name": "@outfitter/file-ops",
      "type": "patch",
      "oldVersion": "0.2.4",
      "changesets": [],
      "newVersion": "0.2.5"
    },
    {
      "name": "@outfitter/index",
      "type": "patch",
      "oldVersion": "0.2.4",
      "changesets": [],
      "newVersion": "0.2.5"
    },
    {
      "name": "@outfitter/logging",
      "type": "none",
      "oldVersion": "0.4.2",
      "changesets": [],
      "newVersion": "0.4.2"
    },
    {
      "name": "@outfitter/schema",
      "type": "patch",
      "oldVersion": "0.2.3",
      "changesets": [],
      "newVersion": "0.2.4"
    },
    {
      "name": "@outfitter/state",
      "type": "patch",
      "oldVersion": "0.2.4",
      "changesets": [],
      "newVersion": "0.2.5"
    },
    {
      "name": "@outfitter/types",
      "type": "patch",
      "oldVersion": "0.2.4",
      "changesets": [],
      "newVersion": "0.2.5"
    }
  ]
}