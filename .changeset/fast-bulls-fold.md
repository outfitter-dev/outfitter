---
"@outfitter/tooling": minor
---

Expand `check-tsdoc` tooling with reusable analysis primitives and richer CLI behaviors consumed by `outfitter check tsdoc`, including summary and filter support, JSONL output, and runner hardening around `cwd` and missing `jq` handling.
