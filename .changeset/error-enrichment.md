---
"@outfitter/contracts": minor
---

Add `jsonRpcCodeMap`, `retryableMap`, and `errorCategoryMeta()` to enrich ErrorCategory with JSON-RPC codes (for MCP protocol compliance) and retryable flags (for agent safety)
