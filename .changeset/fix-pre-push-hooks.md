---
"@outfitter/tooling": patch
---

Fix pre-push hook reliability and performance: add stale process cleanup (OS-532), fix sync:exports oxfmt round-trip convergence (OS-530), and scope pre-push verification to affected packages via change detection (OS-531)
