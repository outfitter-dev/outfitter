---
"@outfitter/docs": minor
---

Add `checkFreshness()` and `refreshIfNeeded()` methods to `DocsSearch`. Export FTS5 query normalization utilities (`isFtsParseError`, `hasFtsSyntax`, `quoteFtsTerms`, `shouldRetryAsQuoted`) from `@outfitter/docs/search`.
