---
"@outfitter/cli": patch
---

Add generic custom flag builders (`booleanFlagPreset`, `enumFlagPreset`, `numberFlagPreset`, `stringListFlagPreset`) for command-local CLI flags, and dogfood them in `outfitter upgrade` action wiring.
