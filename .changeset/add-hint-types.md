---
"@outfitter/contracts": minor
---

Add `ActionHint`, `CLIHint`, and `MCPHint` types for agent-navigable responses. Exported from `@outfitter/contracts` root and `@outfitter/contracts/hints` subpath. Types only — no runtime code.
