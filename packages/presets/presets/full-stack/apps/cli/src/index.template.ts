/**
 * {{projectName}} CLI - Public API.
 *
 * The CLI entry point is src/cli.ts (run directly via bun).
 * This module re-exports core types for programmatic use.
 */

export { createGreeting } from "{{packageName}}-core";
